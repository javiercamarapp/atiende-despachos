# Blueprint de requisitos trazable — Agentes fiscales (candidatos de un contador real)

> Fecha: 2026-09-08. Repo: `github.com/javiercamarapp/atiende-despachos`, rama `main`.

Fuentes: los 3 reportes de investigación a profundidad encargados por Javier sobre features fiscales reales propuestas por un contador —`devolucion-iva`, `migracion-catalogo-cuentas`, `conciliacion-bancaria-n-a-1`— cruzados contra el código real de este repo (lectura completa de los módulos citados en cada reporte, más `grep` exhaustivo para confirmar ausencias). Ningún requisito de este documento describe código que exista y funcione hoy: **todo el estado inicial es `pendiente`**, incluyendo los casos donde ya existe una pieza parcial o mal fundamentada (esos se marcan `pendiente` igual, con la nota de qué existe y qué le falta).

Este documento es de **diseño y planeación** — no se implementó ningún código como parte de esta tarea. Es la matriz de trabajo para las 3 features candidatas, con el mismo rigor que `docs/REQUISITOS.md`/`docs/ACEPTACION.md` de `atiende-hoteles`: un requisito por fila, con un criterio de aceptación observable (valores concretos), tipo de prueba, ubicación prevista del código y del test, comando previsto para correrlo, dependencia de credenciales, y estado.

## 0. Convención de ID canónico

`REQ-<MOD>-nnn`, `MOD ∈ {IVA, MIG, CONC}`:

- **IVA** — Devolución de IVA: papel de trabajo DIOT↔CFDI↔pago, clasificación depósito ingreso-grava vs. no-grava, regla de no discrepancia fiscal (Art. 59 fracc. III CFF).
- **MIG** — Migración/fusión de catálogo de cuentas entre dos bases contables: matching automático, reclasificación asistida, migración de pólizas sin pérdida.
- **CONC** — Conciliación bancaria N-a-1: algoritmo de matching combinatorio (subset-sum) con tolerancia de comisión de terminal/agregador, para cobros y para pagos.

Cada renglón cita, cuando aplica, el artículo legal o el hallazgo concreto de la investigación que lo origina (p. ej. "CFF Art. 59 fracc. III", "LIVA Art. 5 fracc. III", "trámite 21/CFF Anexo 7"). Prioridad: **P0** (bloquea el resto de la feature o es un requisito de integridad/seguridad de datos fiscales), **P1** (necesario para que la feature sea usable por un despacho real), **P2** (mejora / caso de borde no crítico para el primer release).

## 1. Conteos (cuenta real sobre este documento)

| Módulo | Requisitos canónicos | P0 | P1 | P2 |
|---|---|---|---|---|
| IVA | 18 | 9 | 7 | 2 |
| MIG | 18 | 10 | 6 | 2 |
| CONC | 18 | 9 | 7 | 2 |
| **Total** | **54** | **28** | **20** | **6** |

Estado de las 54 filas: **100% `pendiente`** — ninguna de las tres features tiene código de producción hoy (confirmado por lectura completa de los módulos citados en los 3 reportes y por `grep` sin resultados de los términos clave: "subset", "group_id", "N-a-1", "fusión de catálogo", "mapeo_migracion", "settlement_profile" en todo el repo al 2026-09-08).

---

## 2. Matriz de requisitos — REQ-IVA (Devolución de IVA)

Contexto verificado en el repo al momento de este blueprint: `b2b_ai/features/devolucion_iva/` (models.py, service.py, workpaper.py, validators.py, routes.py — 8 endpoints REST montados en `b2b_ai/api/app.py:1075`), `b2b_ai/features/diot/` (catálogo real `TipoOperacion` en models.py), `b2b_ai/features/reconciliacion_ingresos_egresos/` (`ClasificacionDeposito` con `FINANCIAMIENTO`/`APORTACION_SOCIO`/`GARANTIA`, motor de reglas regex en `classification_rules.py`, montado en `app.py:1085`). Todo el almacenamiento actual de ambos módulos es en memoria de proceso (dicts a nivel de módulo o de instancia); no existe tabla en `migrations/` ni en `schema-parcial.sql`.

| ID | Requisito (criterio de aceptación observable) | Tipo de prueba | Código previsto | Test previsto | Comando previsto | Credenciales | Estado |
|---|---|---|---|---|---|---|---|
| REQ-IVA-001 | El fundamento legal de la clasificación "financiamiento" y "aportación de socio" en `classification_rules.py::DEFAULT_RULES` debe corregirse de `articulo_cff="CFF Art. 14"` a `articulo_cff="CFF Art. 59 fracción III"` (excepción a la presunción de depósitos bancarios), y la regla de "garantía" (hoy `articulo_cff=None`) debe poblarse con el mismo artículo. Verificable con `assert regla.articulo_cff == "CFF Art. 59 fracción III"` para las 3 reglas. | unit | `b2b_ai/features/reconciliacion_ingresos_egresos/classification_rules.py` | `tests/features/reconciliacion_ingresos_egresos/test_classification_rules.py` | `pytest tests/features/reconciliacion_ingresos_egresos/test_classification_rules.py -k articulo_cff` | no | pendiente |
| REQ-IVA-002 | El modelo `DepositoBancario`/`ClasificacionDepositoResult` debe ganar los campos `documento_soporte_id` (FK a repositorio de documentos), `fecha_documento` y `estado_recaracterizacion` (`vigente`/`requiere_formalizacion`/`recaracterizado`); ningún depósito clasificado como `FINANCIAMIENTO`, `APORTACION_SOCIO` o `GARANTIA` puede persistirse sin `documento_soporte_id` no nulo. | unit | `b2b_ai/features/reconciliacion_ingresos_egresos/models.py` | `tests/features/reconciliacion_ingresos_egresos/test_models_documento_soporte.py` | `pytest tests/features/reconciliacion_ingresos_egresos/test_models_documento_soporte.py` | no | pendiente |
| REQ-IVA-003 | Debe existir un endpoint (`POST /api/v1/reconciliacion-ingresos-egresos/{id}/documento-soporte`) para adjuntar el documento (contrato de mutuo, acta de asamblea, contrato de garantía) a una clasificación ya hecha; sin ese adjunto, la clasificación queda en estado `pendiente_evidencia` y no puede incluirse en un papel de trabajo exportable. | integration | `b2b_ai/features/reconciliacion_ingresos_egresos/{routes,service}.py` | `tests/features/reconciliacion_ingresos_egresos/test_routes_documento_soporte.py` | `pytest tests/features/reconciliacion_ingresos_egresos/test_routes_documento_soporte.py` | no | pendiente |
| REQ-IVA-004 | `devolucion_iva/service.py::generar_diot()` debe dejar de hardcodear `tipo_operacion="01"` (código inexistente en el catálogo real) y consumir el enum `TipoOperacion` de `b2b_ai/features/diot/models.py` (03=gravada 16%, 05, 06=tasa 0%, 07, 85, etc.); test de regresión: para una factura con IVA 16%, `generar_diot()` debe producir `tipo_operacion == "03"`, nunca `"01"`. | unit | `b2b_ai/features/devolucion_iva/service.py` (consumir `b2b_ai/features/diot/models.py::TipoOperacion`) | `tests/features/devolucion_iva/test_generar_diot_catalogo_real.py` | `pytest tests/features/devolucion_iva/test_generar_diot_catalogo_real.py` | no | pendiente |
| REQ-IVA-005 | Deben crearse migraciones nuevas para 4 tablas persistentes: `devolucion_iva_solicitudes`, `devolucion_iva_papeles_trabajo`, `diot_entries`, `depositos_bancarios_clasificados`, todas con `tenant_id NOT NULL` e índice `(tenant_id, periodo)`; verificable ejecutando la migración contra una BD de pruebas y confirmando `information_schema.columns` para las 4 tablas. | integration | `migrations/versions/0010_devolucion_iva_persistencia.py` | `tests/migrations/test_0010_devolucion_iva_persistencia.py` | `pytest tests/migrations/test_0010_devolucion_iva_persistencia.py` | credenciales de BD de pruebas (Postgres local/CI) | completo (verificado independientemente 8-sep: migración aplicada contra Postgres real, las 4 tablas existen con `tenant_id NOT NULL` y el índice compuesto `(tenant_id, periodo)`, un INSERT sin tenant_id es rechazado con `NotNullViolation` real, downgrade a 0009 limpia las 4 tablas; 4/4 tests nuevos pasan y la cadena completa de migraciones 0001→0010 sigue subiendo/bajando sin regresión (2/2)) |
| REQ-IVA-006 | `DevolucionIVAService` debe leer/escribir `_solicitudes`, `_status` y `_papeles_trabajo` desde las tablas de REQ-IVA-005 en vez de dicts de proceso; verificable reiniciando el proceso entre el `POST` de creación y el `GET` de consulta y confirmando que la solicitud sigue existiendo (hoy se perdería). | integration | `b2b_ai/features/devolucion_iva/service.py` | `tests/features/devolucion_iva/test_persistencia_solicitudes.py` | `pytest tests/features/devolucion_iva/test_persistencia_solicitudes.py` | credenciales de BD de pruebas | pendiente |
| REQ-IVA-007 | `listar_solicitudes(tenant_id=...)` debe filtrar realmente por `tenant_id` (hoy recibe el parámetro y lo ignora, recorriendo todo `_solicitudes`); prueba adversarial: crear solicitudes para `tenant_id="A"` y `tenant_id="B"`, llamar `listar_solicitudes(tenant_id="A")` y confirmar `len(resultado) == 1` y que ninguna fila pertenece a `"B"`. | adversarial | `b2b_ai/features/devolucion_iva/service.py::listar_solicitudes` | `tests/adversarial/test_devolucion_iva_tenant_isolation.py` | `pytest tests/adversarial/test_devolucion_iva_tenant_isolation.py` | no | pendiente |
| REQ-IVA-008 | El modelo `FacturaCFDI` debe ganar `folio_factura` (serie+folio, distinto del UUID), `forma_pago`, `metodo_pago` y `referencia_complemento_pago` (UUID del REP) como campos requeridos para poder marcarse "lista para anexo 7/7-A"; sin `referencia_complemento_pago`, la factura no debe contarse como IVA acreditable "efectivamente pagado" (LIVA Art. 5 fracc. III). | unit | `b2b_ai/features/devolucion_iva/models.py` | `tests/features/devolucion_iva/test_models_anexo7.py` | `pytest tests/features/devolucion_iva/test_models_anexo7.py` | no | pendiente |
| REQ-IVA-009 | El `WorkpaperGenerator` debe ganar una sección 7 ("no discrepancia fiscal — depósitos bancarios") que incorpore el `PapelTrabajoConciliacion` de `reconciliacion_ingresos_egresos` (clasificaciones de depósito + `articulo_cff` de REQ-IVA-001), expuesta en un endpoint combinado `GET /api/v1/devolucion-iva/papel-trabajo-completo/{periodo}` que devuelve 7 secciones, no 6. | integration | `b2b_ai/features/devolucion_iva/workpaper.py`, `routes.py` | `tests/features/devolucion_iva/test_papel_trabajo_completo.py` | `pytest tests/features/devolucion_iva/test_papel_trabajo_completo.py` | no | pendiente |
| REQ-IVA-010 | Antes de generar una `SolicitudDevolucion` con `monto_solicitado > 10001.00` MXN, el servicio debe correr una validación automática de congruencia DIOT↔CFDI↔declaración mensual (comparar totales agrupados) y, si la diferencia supera `$1.00` MXN (redondeo) o si la DIOT del periodo no existe, marcar la solicitud como `estado="requiere_aclaracion"` en vez de `"lista_para_envio"`. | unit | `b2b_ai/features/devolucion_iva/service.py::preparar_solicitud` | `tests/features/devolucion_iva/test_congruencia_umbral_10001.py` | `pytest tests/features/devolucion_iva/test_congruencia_umbral_10001.py` | no | pendiente |
| REQ-IVA-011 | El sistema nunca debe negar ni reducir automáticamente una devolución de IVA solo por depósitos bancarios clasificados como sospechosos (sin documento de soporte o con score bajo); ante esa condición debe marcar el caso como `requiere_revision_humana=True` con la nota "Art. 59 fr. III CFF exige facultades de comprobación previas (PRODECON 1/2026)" y nunca setear `estado="rechazada"` automáticamente. Ver ADR-4. | adversarial | `b2b_ai/features/reconciliacion_ingresos_egresos/service.py` | `tests/adversarial/test_no_negacion_automatica_por_deposito.py` | `pytest tests/adversarial/test_no_negacion_automatica_por_deposito.py` | no | pendiente |
| REQ-IVA-012 | Corregir el bug de código muerto: `conciliar_depositos_auxiliares()` debe asignar el resultado a `self._conciliaciones[conciliacion_id]` antes de retornar; prueba de regresión: llamar `conciliar_depositos_auxiliares(...)` y luego `get_conciliacion(conciliacion_id)` y confirmar que NO devuelve `None`. | unit | `b2b_ai/features/reconciliacion_ingresos_egresos/service.py` | `tests/features/reconciliacion_ingresos_egresos/test_get_conciliacion_no_none.py` | `pytest tests/features/reconciliacion_ingresos_egresos/test_get_conciliacion_no_none.py` | no | pendiente |
| REQ-IVA-013 | Cada clasificación de depósito no trivial (financiamiento/aportación/garantía) debe registrar `clasificado_por` y `clasificado_en` (timestamp), y el motor de reglas por regex debe marcarse siempre como `origen="automatico_sugerido"`, nunca `"aprobado"`, hasta que un humano confirme vía `PATCH /clasificaciones/{id}/aprobar`. | integration | `b2b_ai/features/reconciliacion_ingresos_egresos/{models,routes,service}.py` | `tests/features/reconciliacion_ingresos_egresos/test_aprobacion_humana_clasificacion.py` | `pytest tests/features/reconciliacion_ingresos_egresos/test_aprobacion_humana_clasificacion.py` | no | pendiente |
| REQ-IVA-014 | El `FED` (Formato Electrónico de Devoluciones) exportable debe incluir, por cada línea de proveedor, los 9 campos mínimos del anexo 7/7-A (RFC, nombre/razón social, folio fiscal, folio de factura, fecha de factura, fecha de pago, forma de pago, banco de pago, IVA trasladado/acreditable); prueba de esquema: el JSON/CSV exportado debe tener exactamente esas 9 claves no nulas por fila. | unit | `b2b_ai/features/devolucion_iva/workpaper.py` | `tests/features/devolucion_iva/test_export_anexo7_esquema.py` | `pytest tests/features/devolucion_iva/test_export_anexo7_esquema.py` | no | pendiente |
| REQ-IVA-015 | La CLABE de 18 dígitos capturada para el depósito de la devolución debe validarse con el algoritmo de dígito verificador (módulo 10 ponderado 3-7-1) antes de aceptar la solicitud; una CLABE con dígito verificador incorrecto debe rechazarse con `ValueError` y nunca persistirse. | unit | `b2b_ai/features/devolucion_iva/validators.py` | `tests/features/devolucion_iva/test_validar_clabe.py` | `pytest tests/features/devolucion_iva/test_validar_clabe.py` | no | pendiente |
| REQ-IVA-016 | El plazo de 40 días hábiles (20 si hay dictamen/garantía, Art. 22 CFF) debe calcularse y exponerse en `GET /papel-trabajo/{periodo}` como `fecha_limite_resolucion`, contando solo días hábiles (excluyendo sábados, domingos y el calendario oficial de días inhábiles del SAT publicado en el Anexo correspondiente de la RMF vigente); prueba con una solicitud presentada un jueves debe dar una fecha 40 días hábiles después, no 40 días naturales. | unit | `b2b_ai/features/devolucion_iva/service.py` | `tests/features/devolucion_iva/test_plazo_40_dias_habiles.py` | `pytest tests/features/devolucion_iva/test_plazo_40_dias_habiles.py` | no | pendiente |
| REQ-IVA-017 | Debe existir un contract test que descargue (o reciba mockeado con fixture etiquetada como tal) el catálogo real de tipos de operación DIOT vigente (Anexo 19 RMF) y confirme que los códigos usados en `diot/models.py::TipoOperacion` siguen vigentes; hasta tener la fuente oficial conectada, el test queda `xfail` documentado, nunca se asume "vigente" sin verificar. | contract | `b2b_ai/features/diot/models.py` | `tests/contract/test_diot_catalogo_vigente.py` | `pytest tests/contract/test_diot_catalogo_vigente.py` | sí — acceso a fuente oficial SAT/RMF vigente | pendiente |
| REQ-IVA-018 | El endpoint `POST /api/v1/devolucion-iva/conciliar` debe rechazar (HTTP 422) una solicitud cuyo `tenant_id` del token no coincida con el `tenant_id` de las facturas/DIOT/declaraciones enviadas en el body, para blindar el aislamiento multi-tenant también a nivel de payload, no solo de lectura (complementa REQ-IVA-007). | adversarial | `b2b_ai/features/devolucion_iva/routes.py` | `tests/adversarial/test_devolucion_iva_payload_tenant_mismatch.py` | `pytest tests/adversarial/test_devolucion_iva_payload_tenant_mismatch.py` | no | pendiente |

---

## 3. Matriz de requisitos — REQ-MIG (Migración/fusión de catálogo de cuentas)

Contexto verificado: no existe ningún archivo, tabla, endpoint ni test de "fusión"/"migración de catálogo"/"merge"/"matching de catálogo" en el repo. Existen tres modelos paralelos y no unificados de catálogo/póliza (`b2b_ai/features/contabilidad/models.py`, `b2b_ai/features/bookkeeping/models.py`, `b2b_ai/integrations/erp/models.py`), la tabla `asientos_contables.cuenta_debito/cuenta_credito` es `TEXT` plano sin FK, y `ContabilidadService.cargar_catalogo()` (línea 306) sobrescribe el catálogo completo sin merge. El motor de matching progresivo más reutilizable del repo es `b2b_ai/features/reconciliation_agent/matching_engine.py` (rapidfuzz, 4 niveles, ya probado para conciliación bancaria).

| ID | Requisito (criterio de aceptación observable) | Tipo de prueba | Código previsto | Test previsto | Comando previsto | Credenciales | Estado |
|---|---|---|---|---|---|---|---|
| REQ-MIG-001 | Debe crearse una migración que agregue `cuenta_id UUID PRIMARY KEY` a `cuentas_contables` (si no existe ya como PK) y una columna `cuenta_id UUID REFERENCES cuentas_contables(id)` en `asientos_contables` para reemplazar `cuenta_debito`/`cuenta_credito` como TEXT; verificable con `\d asientos_contables` mostrando la FK activa. | integration | `migrations/versions/00XX_cuenta_id_fk.py` | `tests/migrations/test_00XX_cuenta_id_fk.py` | `pytest tests/migrations/test_00XX_cuenta_id_fk.py` | credenciales de BD de pruebas | pendiente |
| REQ-MIG-002 | Debe crearse el módulo `b2b_ai/features/migracion_catalogo/` (nuevo feature) con modelo `MapeoMigracionCuenta`: `origen_cuenta_id`, `destino_cuenta_id`, `tipo_match` (`exacto`/`alerta_riesgo`/`fuzzy`/`sin_match`), `score` (0-100), `estado` (`pendiente`/`aprobado`/`rechazado`/`editado`), `aprobado_por`, `aprobado_en`, `nota`; verificable con un test de esquema que instancie el modelo con los 9 campos. | unit | `b2b_ai/features/migracion_catalogo/models.py` | `tests/features/migracion_catalogo/test_models.py` | `pytest tests/features/migracion_catalogo/test_models.py` | no | pendiente |
| REQ-MIG-003 | El motor de matching debe auto-aprobar (sin cola de revisión) únicamente cuando código normalizado (sin acentos/mayúsculas/espacios colapsados) Y nombre normalizado sean idénticos entre origen y destino; prueba: 100 pares código+nombre idénticos → 100 mapeos con `tipo_match="exacto"` y `estado="aprobado"` sin intervención humana. | unit | `b2b_ai/features/migracion_catalogo/matching.py` | `tests/features/migracion_catalogo/test_matching_exacto.py` | `pytest tests/features/migracion_catalogo/test_matching_exacto.py` | no | pendiente |
| REQ-MIG-004 | El motor debe marcar como `tipo_match="alerta_riesgo"` (nunca auto-aprobado) todo par donde el código coincide pero el nombre difiere, o el nombre coincide pero el código difiere; prueba: cuenta origen `code="102-001", name="Bancos"` vs. destino `code="102-001", name="Clientes"` debe producir `estado="pendiente"`, jamás `"aprobado"`. | unit | `b2b_ai/features/migracion_catalogo/matching.py` | `tests/features/migracion_catalogo/test_matching_alerta_riesgo.py` | `pytest tests/features/migracion_catalogo/test_matching_alerta_riesgo.py` | no | pendiente |
| REQ-MIG-005 | El motor debe calcular un `tipo_match="fuzzy"` con `score` compuesto (similitud de nombre vía rapidfuzz `token_sort_ratio`, ponderada con coincidencia de nivel jerárquico, naturaleza D/A, tipo agregado y cuenta padre) para pares sin match exacto ni alerta de riesgo; todo resultado fuzzy debe quedar con `estado="pendiente"` sin excepción, incluso con `score=99`. | unit | `b2b_ai/features/migracion_catalogo/matching.py` (adaptando `reconciliation_agent/matching_engine.py`) | `tests/features/migracion_catalogo/test_matching_fuzzy_score.py` | `pytest tests/features/migracion_catalogo/test_matching_fuzzy_score.py` | no | pendiente |
| REQ-MIG-006 | Una cuenta origen sin ningún candidato con `score >= 60` debe quedar clasificada como `tipo_match="sin_match"` con nota `"cuenta nueva a crear en destino"`, y nunca generar un `MapeoMigracionCuenta` con `destino_cuenta_id` inventado. | unit | `b2b_ai/features/migracion_catalogo/matching.py` | `tests/features/migracion_catalogo/test_matching_sin_match.py` | `pytest tests/features/migracion_catalogo/test_matching_sin_match.py` | no | pendiente |
| REQ-MIG-007 | Debe existir un endpoint `POST /api/v1/migracion-catalogo/{mapeo_id}/aprobar` (y `/rechazar`, `/editar`) que sea el ÚNICO camino para pasar un mapeo de `alerta_riesgo` o `fuzzy` a `estado="aprobado"`; prueba adversarial: invocar el motor de migración de pólizas (REQ-MIG-009) directamente sobre un mapeo en `estado="pendiente"` debe fallar con excepción explícita, nunca migrar. Ver ADR-3. | adversarial | `b2b_ai/features/migracion_catalogo/{routes,service}.py` | `tests/adversarial/test_migracion_requiere_aprobacion_explicita.py` | `pytest tests/adversarial/test_migracion_requiere_aprobacion_explicita.py` | no | pendiente |
| REQ-MIG-008 | Un mapeo N:1 (varias cuentas origen apuntando al mismo `destino_cuenta_id`) debe requerir que el contador confirme explícitamente cómo se concilian los saldos (campo `estrategia_conciliacion_saldos` obligatorio no nulo antes de aprobar); un mapeo 1:N debe rechazarse por completo (HTTP 422, "división 1:N fuera de alcance automático"). | integration | `b2b_ai/features/migracion_catalogo/service.py` | `tests/features/migracion_catalogo/test_mapeo_n_a_1_y_1_a_n.py` | `pytest tests/features/migracion_catalogo/test_mapeo_n_a_1_y_1_a_n.py` | no | pendiente |
| REQ-MIG-009 | El motor de migración de pólizas debe ser transaccional por póliza completa: si alguna línea de una póliza no tiene mapeo aprobado, la póliza ENTERA se aborta (0 líneas migradas de esa póliza) y se manda a una cola `polizas_bloqueadas`; nunca debe migrar parcialmente una póliza dejando débito≠crédito en destino. | integration | `b2b_ai/features/migracion_catalogo/migrador.py` | `tests/features/migracion_catalogo/test_migracion_atomica_por_poliza.py` | `pytest tests/features/migracion_catalogo/test_migracion_atomica_por_poliza.py` | credenciales de BD de pruebas | pendiente |
| REQ-MIG-010 | La migración de pólizas debe ser idempotente: cada póliza origen migrada debe marcarse `migrada_a_id=<id_destino>`; reintentar la migración sobre las mismas pólizas debe producir 0 pólizas nuevas duplicadas en destino (verificable comparando el conteo de pólizas en destino antes/después del segundo intento). | integration | `b2b_ai/features/migracion_catalogo/migrador.py` | `tests/features/migracion_catalogo/test_migracion_idempotente.py` | `pytest tests/features/migracion_catalogo/test_migracion_idempotente.py` | credenciales de BD de pruebas | pendiente |
| REQ-MIG-011 | El migrador nunca debe emitir `DELETE` sobre las pólizas/cuentas de origen; prueba estática tipo `scripts/checks/no-delete-events.ts` (equivalente Python) que falle el build si aparece un `DELETE FROM` en `migracion_catalogo/migrador.py` contra las tablas de origen. | unit (chequeo estático) | `scripts/checks/no_delete_origen_migracion.py` | `tests/checks/test_no_delete_origen_migracion.py` | `python scripts/checks/no_delete_origen_migracion.py` | no | pendiente |
| REQ-MIG-012 | Verificación de integridad post-migración — cuadre de saldos: para cada cuenta migrada, `abs(saldo_destino_periodo - saldo_origen_periodo) <= 0.01` MXN; una diferencia mayor debe bloquear el cierre de la migración con un reporte de las cuentas que no cuadraron. | integration | `b2b_ai/features/migracion_catalogo/verificacion.py` | `tests/features/migracion_catalogo/test_verificacion_cuadre_saldos.py` | `pytest tests/features/migracion_catalogo/test_verificacion_cuadre_saldos.py` | credenciales de BD de pruebas | pendiente |
| REQ-MIG-013 | Verificación de integridad — conteo de pólizas: `count(polizas_destino_migradas) == count(polizas_origen_elegibles)`, exacto (0 de diferencia); cualquier discrepancia bloquea el cierre de la migración. | integration | `b2b_ai/features/migracion_catalogo/verificacion.py` | `tests/features/migracion_catalogo/test_verificacion_conteo_polizas.py` | `pytest tests/features/migracion_catalogo/test_verificacion_conteo_polizas.py` | credenciales de BD de pruebas | pendiente |
| REQ-MIG-014 | Verificación de integridad — balance por póliza: el 100% de las pólizas migradas a destino debe cumplir `sum(debe) == sum(haber)` con tolerancia `0.00` (no `0.01`, porque el balance ya venía cuadrado en origen y el remapeo de cuenta no debe alterar montos). | integration | `b2b_ai/features/migracion_catalogo/verificacion.py` | `tests/features/migracion_catalogo/test_verificacion_balance_poliza.py` | `pytest tests/features/migracion_catalogo/test_verificacion_balance_poliza.py` | credenciales de BD de pruebas | pendiente |
| REQ-MIG-015 | Verificación de integridad — referencias huérfanas: 0 líneas de póliza en destino deben apuntar a un `cuenta_id` inexistente en el catálogo destino; el chequeo corre como parte del cierre de migración y bloquea si `count(huerfanas) > 0`. | integration | `b2b_ai/features/migracion_catalogo/verificacion.py` | `tests/features/migracion_catalogo/test_verificacion_referencias_huerfanas.py` | `pytest tests/features/migracion_catalogo/test_verificacion_referencias_huerfanas.py` | credenciales de BD de pruebas | pendiente |
| REQ-MIG-016 | Log de auditoría append-only: cada aprobación/rechazo/edición de un `MapeoMigracionCuenta` debe insertar una fila inmutable en `migracion_catalogo_audit_log` (quién, cuándo, mapeo, decisión, nota); el log nunca acepta `UPDATE` ni `DELETE` (verificable con permisos de BD o trigger que los rechace). | integration | `migrations/versions/00XX_migracion_audit_log.py`, `b2b_ai/features/migracion_catalogo/service.py` | `tests/features/migracion_catalogo/test_audit_log_append_only.py` | `pytest tests/features/migracion_catalogo/test_audit_log_append_only.py` | credenciales de BD de pruebas | pendiente |
| REQ-MIG-017 | `ContabilidadService.cargar_catalogo()` debe dejar de sobrescribir destructivamente el catálogo (comportamiento actual, línea 306) y en su lugar hacer *merge* por `cuenta_id`/código normalizado: cuentas nuevas se agregan, cuentas existentes se actualizan solo si difieren, ninguna cuenta existente se elimina por una carga; prueba: cargar un catálogo de 10 cuentas sobre uno existente de 15 debe resultar en ≥15 cuentas, nunca en 10. | unit | `b2b_ai/features/contabilidad/service.py::cargar_catalogo` | `tests/features/contabilidad/test_cargar_catalogo_merge_no_destructivo.py` | `pytest tests/features/contabilidad/test_cargar_catalogo_merge_no_destructivo.py` | no | pendiente |
| REQ-MIG-018 | Debe existir un contract test end-to-end con datos sintéticos de dos catálogos reales (2020-2023 / 2024-2026, ≥50 cuentas cada uno con al menos 5 casos de cada tipo de match) que corra el pipeline completo (match → cola de revisión simulada con aprobaciones fijas → migración → verificación) y termine con `0` discrepancias de saldo y `0` pólizas huérfanas. | e2e | `b2b_ai/features/migracion_catalogo/` (pipeline completo) | `tests/e2e/test_migracion_catalogo_end_to_end.py` | `pytest tests/e2e/test_migracion_catalogo_end_to_end.py -m e2e` | credenciales de BD de pruebas | pendiente |

---

## 4. Matriz de requisitos — REQ-CONC (Conciliación bancaria N-a-1)

Contexto verificado: `b2b_ai/services/bank_reconciliation.py` (`BankReconciliation.match_transactions`, 3 pases secuenciales `_pass_exact`→`_pass_partial`→`_pass_ai`, todos estrictamente 1 factura : 1 movimiento vía `_build_match`); `b2b_ai/services/reconcile.py` tiene la misma limitación de bajo nivel. `grep` exhaustivo sin resultados de "subset", "group", "N-a-1", "comisión de terminal", "Clip" en todo el repo.

| ID | Requisito (criterio de aceptación observable) | Tipo de prueba | Código previsto | Test previsto | Comando previsto | Credenciales | Estado |
|---|---|---|---|---|---|---|---|
| REQ-CONC-001 | Debe crearse el modelo `SettlementProfile` (`tasa_comision_min`, `tasa_comision_max`, `iva_comision`, `liquidacion_dias_habiles_min`, `liquidacion_dias_habiles_max`, `incluye_fin_de_semana_en_lunes`) con semillas para `clip` (`0.036`-`0.043`, sin MSI), `banorte_tpv` (`0.015`-`0.036`, configurable por contrato) y `spei_transferencia`/`cheque` (`0.0`-`0.0`); verificable listando los 3 perfiles semilla por nombre exacto. | unit | `b2b_ai/services/settlement_profiles.py` | `tests/services/test_settlement_profiles_seeds.py` | `pytest tests/services/test_settlement_profiles_seeds.py` | no | completo (auditado, 17/17 tests pasan) |
| REQ-CONC-002 | Debe implementarse `_business_days_window(fecha_banco, perfil)` que calcule la ventana de captura en DÍAS HÁBILES (excluyendo sábado/domingo), con corrimiento cuando `fecha_banco` cae lunes (la ventana debe alcanzar hasta el viernes/sábado/domingo anteriores) y un buffer configurable de 1-2 días hábiles para feriados bancarios; prueba: `fecha_banco` = lunes 2026-09-07, perfil Clip (T+1 hábil) → ventana debe incluir el viernes 2026-09-04 y el fin de semana 05-06. | unit | `b2b_ai/services/bank_reconciliation.py::_business_days_window` | `tests/services/test_business_days_window.py` | `pytest tests/services/test_business_days_window.py` | no | completo (verificado independientemente 9-sep: pruebas reales pasan, suite completa de conciliación 487/487 sin regresión, comiteado en 2670fe5) |
| REQ-CONC-003 | Debe implementarse `_pass_group()` como nuevo pase de `BankReconciliation.match_transactions()`, insertado DESPUÉS de `_pass_exact` y ANTES de `_pass_partial`, consumiendo `free_inv`/`stmt` vía el mismo `_consumed()` que los demás pases; prueba: con 3 facturas ($3,556.00 + $6,796.00 + $9,840.00 = $20,192.00) y un depósito bancario de $20,192.00, el resultado debe incluir 3 filas de match con el mismo `group_id` y el mismo `transaction_id`, con `method="grouped_n_a_1"`. | unit | `b2b_ai/services/bank_reconciliation.py::_pass_group` | `tests/services/test_pass_group_subset_sum.py` | `pytest tests/services/test_pass_group_subset_sum.py` | no | completo (verificado independientemente 9-sep: pruebas reales pasan, suite completa de conciliación 487/487 sin regresión, comiteado en 2670fe5) |
| REQ-CONC-004 | El subset-sum debe trabajar en CENTAVOS (enteros, nunca floats) usando DP de sumas alcanzables con backpointers, aceptando un subconjunto como candidato si `A <= sum(S) <= A / (1 - r_min)` (grossing-up desde el neto del depósito hasta el bruto máximo según `r_min` del perfil); prueba: depósito neto $20,192.00 con perfil Clip (`r_min=0.036`) debe aceptar un subconjunto con suma bruta hasta `$20,192.00 / (1-0.036) = $20,946.60`, y rechazar uno de $21,500.00. | unit | `b2b_ai/services/subset_sum.py` | `tests/services/test_subset_sum_banda_comision.py` | `pytest tests/services/test_subset_sum_banda_comision.py` | no | completo (verificado independientemente 9-sep: pruebas reales pasan, suite completa de conciliación 487/487 sin regresión, comiteado en 2670fe5) |
| REQ-CONC-005 | El algoritmo debe excluir subconjuntos de tamaño 1 (ya cubiertos por `_pass_exact`) y limitar el tamaño máximo de subconjunto a un techo configurable (default `15`); prueba: con 20 facturas candidatas en la ventana, ningún subconjunto propuesto debe tener más de 15 elementos ni exactamente 1. | unit | `b2b_ai/services/subset_sum.py` | `tests/services/test_subset_sum_limites_tamano.py` | `pytest tests/services/test_subset_sum_limites_tamano.py` | no | completo (verificado independientemente 9-sep: pruebas reales pasan, suite completa de conciliación 487/487 sin regresión, comiteado en 2670fe5) |
| REQ-CONC-006 | Cuando el número de candidatos filtrados por ventana supere 40, el algoritmo debe usar meet-in-the-middle (partir en dos mitades, generar sumas de cada mitad, ordenar una y buscar en la otra por rango) en vez de DP de fuerza bruta; prueba de rendimiento: con 60 candidatos sintéticos, el cálculo debe completarse en menos de 5 segundos en CI (marcador `@pytest.mark.slow` si excede eso, nunca bloquear el pipeline sin límite de tiempo). | unit | `b2b_ai/services/subset_sum.py::_meet_in_the_middle` | `tests/services/test_subset_sum_meet_in_the_middle.py` | `pytest tests/services/test_subset_sum_meet_in_the_middle.py` | no | completo (verificado independientemente 9-sep: pruebas reales pasan, suite completa de conciliación 487/487 sin regresión, comiteado en 2670fe5) |
| REQ-CONC-007 | El sistema debe reportar TODOS los subconjuntos cuya suma cae dentro de la banda `[A, A_grossed_up]`, no solo el primero encontrado; prueba: un caso sintético con 2 subconjuntos disjuntos válidos para el mismo depósito debe devolver una lista de longitud 2, nunca truncar a 1 en silencio. | unit | `b2b_ai/services/subset_sum.py` | `tests/services/test_subset_sum_todos_los_candidatos.py` | `pytest tests/services/test_subset_sum_todos_los_candidatos.py` | no | completo (verificado independientemente 9-sep: pruebas reales pasan, suite completa de conciliación 487/487 sin regresión, comiteado en 2670fe5) |
| REQ-CONC-008 | Cuando `_pass_group()` encuentra exactamente 1 candidato válido con score de desambiguación `>= 85` (ver REQ-CONC-009), el match se auto-confirma (`confidence="alta"`, `requiere_confirmacion_humana=False`); en cualquier otro caso (0, o ≥2 candidatos, o score < 85) el resultado queda `estado="sugerido"` y **nunca se auto-aplica**. Ver ADR-2. | adversarial | `b2b_ai/services/bank_reconciliation.py::_pass_group` | `tests/adversarial/test_pass_group_ambiguedad_nunca_autoaplica.py` | `pytest tests/adversarial/test_pass_group_ambiguedad_nunca_autoaplica.py` | no | completo (auditado, 8/8 tests pasan; regresión verificada en tests/services/test_pass_group_subset_sum.py, test_subset_sum_limites_tamano.py, test_build_group_match.py, tests/test_bank_reconciliation.py, tests/test_bank_reconciliation_v2.py — 157+ tests, 0 fallos) |
| REQ-CONC-009 | Debe implementarse un score de desambiguación compuesto (0-100) que pondere: cercanía de la comisión implícita del subconjunto a la tasa nominal del perfil, compacidad de fechas dentro del grupo (menor dispersión = mejor), tamaño del grupo (menos facturas = mejor, salvo evidencia contraria) y unicidad (único candidato en la banda = score más alto); prueba: dos subconjuntos con la misma suma pero distinta dispersión de fechas deben producir scores distintos, nunca empatados por diseño salvo que sean idénticos en las 4 dimensiones. | unit | `b2b_ai/services/group_scoring.py` | `tests/services/test_group_scoring.py` | `pytest tests/services/test_group_scoring.py` | no | completo (auditado, 17/17 tests pasan; regresión verificada en tests/services/ + tests/test_bank_reconciliation*.py — 586 tests, 0 fallos) |
| REQ-CONC-010 | `_build_group_match()` debe generar N filas de match (una por factura del subconjunto) compartiendo el mismo `transaction_id` y `group_id`, cada una con el `method="grouped_n_a_1"`; prueba de esquema: para un grupo de 3 facturas, el resultado debe tener 3 filas con `transaction_id` idéntico y `group_id` idéntico, y `invoice_ref` distinto en cada una. | unit | `b2b_ai/services/bank_reconciliation.py::_build_group_match` | `tests/services/test_build_group_match.py` | `pytest tests/services/test_build_group_match.py` | no | completo (auditado independientemente, 6/6 tests pasan; esquema N filas / mismo transaction_id y group_id / method="grouped_n_a_1" / invoice_ref distinto verificado leyendo el código y ejecutando la prueba; regresión de todo el módulo de conciliación 594/594 en el momento de esta auditoría) |
| REQ-CONC-011 | `generate_reconciliation_report()` debe sumar el "monto conciliado" agrupando por `transaction_id` único (no por fila de match), para no contar el mismo depósito varias veces cuando hay `group_id`; prueba: un depósito de $20,192.00 conciliado contra 3 facturas debe sumar $20,192.00 una sola vez al total conciliado, no $20,192.00 × 3. | unit | `b2b_ai/services/bank_reconciliation.py::generate_reconciliation_report` | `tests/services/test_reporte_no_duplica_monto_por_grupo.py` | `pytest tests/services/test_reporte_no_duplica_monto_por_grupo.py` | no | pendiente |
| REQ-CONC-012 | El mismo pase debe aplicarse simétricamente a egresos (`naturaleza="cargo"`) contra comprobantes/pagos programados pendientes, parametrizado por perfil de comisión de dispersión (default `0%` para SPEI saliente salvo que el perfil del banco emisor indique cuota); prueba: un cargo agrupado de nómina dispersado a 5 empleados debe generar 5 filas de match con `group_id` compartido, igual que en cobros. | unit | `b2b_ai/services/bank_reconciliation.py::_pass_group` (parametrizado por `naturaleza`) | `tests/services/test_pass_group_egresos.py` | `pytest tests/services/test_pass_group_egresos.py` | no | pendiente |
| REQ-CONC-013 | Debe crearse una migración para la tabla `reconciliation_group_matches` (`group_id`, `transaction_id`, `tenant_id`, `naturaleza`, `confidence`, `estado`, `score`, `aprobado_por`, `aprobado_en`), revisando primero el esquema real de `migrations/versions/0005_bank_reconciliation_state.py` para no duplicar columnas ya existentes. | integration | `migrations/versions/00XX_reconciliation_group_matches.py` | `tests/migrations/test_00XX_reconciliation_group_matches.py` | `pytest tests/migrations/test_00XX_reconciliation_group_matches.py` | credenciales de BD de pruebas | pendiente |
| REQ-CONC-014 | Cuando ningún subconjunto cae dentro de la banda `[A, A_grossed_up]` para un movimiento bancario, el sistema debe dejarlo explícitamente en `estado="sin_conciliar"` con el detalle de los candidatos más cercanos evaluados (para trazabilidad); el sistema **nunca** debe asignar automáticamente el candidato de menor diferencia si ese candidato está fuera de la banda de tolerancia. Ver ADR-1. | adversarial | `b2b_ai/services/bank_reconciliation.py::_pass_group` | `tests/adversarial/test_pass_group_nunca_inventa_match.py` | `pytest tests/adversarial/test_pass_group_nunca_inventa_match.py` | no | pendiente |
| REQ-CONC-015 | El filtrado de candidatos debe acotarse por `canal_cobro` (terminal/SPEI/cheque/efectivo) e `id_terminal`/cuenta destino cuando el dato exista en la factura/cobro pendiente, antes de correr el subset-sum, para mantener el universo de candidatos en un tamaño tratable (`<= 50` típico); prueba: con facturas de 2 terminales distintas mezcladas, el subset-sum para el depósito de la terminal A nunca debe considerar facturas marcadas de la terminal B. | unit | `b2b_ai/services/bank_reconciliation.py::_pass_group` | `tests/services/test_filtrado_por_canal_terminal.py` | `pytest tests/services/test_filtrado_por_canal_terminal.py` | no | pendiente |
| REQ-CONC-016 | Debe añadirse el campo `canal_cobro` (`terminal`/`spei`/`cheque`/`efectivo`) e `id_terminal` opcional al modelo de factura/cobro pendiente que alimenta la conciliación (`Invoice`/`Collection`), sin romper los tests existentes de `tests/test_bank_reconciliation.py` y `tests/test_bank_reconciliation_v2.py` (deben seguir pasando 100% tras el cambio). | unit | `b2b_ai/services/collections.py` (o el modelo de factura que use `bank_reconciliation.py`) | `tests/services/test_invoice_canal_cobro_field.py` | `pytest tests/services/test_invoice_canal_cobro_field.py tests/test_bank_reconciliation.py tests/test_bank_reconciliation_v2.py` | no | pendiente |
| REQ-CONC-017 | El umbral de auto-confirmación del pase de grupo (`score >= 85` Y subconjunto único, REQ-CONC-008) debe ser estrictamente más alto y más estricto que el umbral de auto-confirmación del pase 1-a-1 existente (`_pass_partial`, que hoy no tiene un score explícito comparable pero opera con tolerancia de monto del 5% sin desambiguación); documentar y verificar con un test de regresión que el pase de grupo nunca auto-confirme un caso que el pase 1-a-1 dejaría en revisión manual bajo el mismo dato de entrada. | unit | `b2b_ai/services/bank_reconciliation.py` | `tests/services/test_umbral_grupo_mas_estricto_que_1a1.py` | `pytest tests/services/test_umbral_grupo_mas_estricto_que_1a1.py` | no | pendiente |
| REQ-CONC-018 | Debe existir un test end-to-end con datos sintéticos de un mes completo de liquidaciones Clip (≥30 depósitos agrupados, ≥150 facturas candidatas, incluyendo al menos 3 casos de ambigüedad real de 2 subconjuntos válidos) que corra el pipeline completo (`match_transactions` con `_pass_group` activo) y confirme: 0 facturas contadas dos veces, 100% de los casos ambiguos en `estado="sugerido"`, y el resto por encima del umbral en `confidence="alta"`. | e2e | `b2b_ai/services/bank_reconciliation.py` (pipeline completo) | `tests/e2e/test_conciliacion_n_a_1_mes_completo.py` | `pytest tests/e2e/test_conciliacion_n_a_1_mes_completo.py -m e2e` | no (datos sintéticos, sin credencial real de Clip/Banorte) | pendiente |

---

## 5. Requisitos que dependen de credenciales o servicios externos (nunca declarar completos con un mock)

| Dependencia | Requisitos afectados |
|---|---|
| BD de pruebas (Postgres local/CI, para migraciones y verificación de integridad transaccional) | REQ-IVA-005, REQ-IVA-006, REQ-MIG-001, REQ-MIG-009, REQ-MIG-010, REQ-MIG-012, REQ-MIG-013, REQ-MIG-014, REQ-MIG-015, REQ-MIG-016, REQ-MIG-018, REQ-CONC-013 |
| Fuente oficial SAT/RMF vigente (catálogo DIOT Anexo 19, calendario de días inhábiles) | REQ-IVA-017 (indirectamente, vía REQ-IVA-016 si se requiere el calendario oficial en vez de solo sábado/domingo) |
| Repositorio de documentos/adjuntos (almacenamiento de contrato de mutuo, acta de asamblea, contrato de garantía) | REQ-IVA-002, REQ-IVA-003 |

Nota: a diferencia de `atiende-hoteles`, la mayoría de los requisitos de este blueprint (matching, subset-sum, verificación de integridad, motor de reglas) son deterministas y verificables con datos sintéticos sin credencial de terceros — la dependencia dominante aquí es una **base de datos de pruebas real** para validar persistencia/transaccionalidad, no un proveedor externo. Los únicos requisitos que si dependen de una fuente oficial externa quedan explícitamente marcados arriba; ningún otro requisito de este documento debe reportarse como completado usando solo mocks si su fila lo marca con credencial.

---

## 6. ADRs — Decisiones de arquitectura no triviales

Mismo formato que `docs/ARQUITECTURA-5-AGENTES.md` §4.2 (Decisión / Por qué / Alternativas descartadas).

### ADR-1 — Representación de "no encontré combinación exacta": nunca inventar un match

**Decisión:** cuando ningún subconjunto de facturas cae dentro de la banda de tolerancia derivada de comisión para un depósito bancario (REQ-CONC-014), el resultado es un estado explícito `estado="sin_conciliar"` con el detalle de los candidatos más cercanos evaluados y por qué fueron descartados (score, diferencia de monto, fecha fuera de ventana). El sistema nunca asigna el candidato de menor diferencia "porque es lo más parecido" si ese candidato no cumple la banda.

**Por qué:**
- Un falso positivo de conciliación (marcar como pagada una factura que no lo fue, o viceversa) es más caro de deshacer que dejar un movimiento pendiente de revisión manual — puede alterar IVA/ISR ya declarado si el error llega a un cierre mensual.
- El mismo principio aplica al matching de catálogo de cuentas (§3, REQ-MIG-006): sin candidato con score suficiente, la cuenta se marca "nueva a crear en destino", nunca se le fuerza un mapeo al candidato más parecido disponible.
- Es consistente con el patrón ya usado en `docs/ARQUITECTURA-5-AGENTES.md` para otras decisiones fiscales de esta base de código: preferir "no sé" explícito sobre una inferencia no verificada.

**Alternativas descartadas:**
- Asignar siempre el candidato de menor diferencia absoluta, marcado con confianza baja: descartada porque un contador que confía en el dashboard puede no revisar cada fila de baja confianza, y una asignación incorrecta silenciosa es peor que una fila vacía visible.
- Dejar el umbral de tolerancia configurable sin piso mínimo: descartada porque permitiría, sin darse cuenta, ensanchar la banda hasta que "todo matchee", perdiendo la garantía de que un match reportado realmente cuadra contra la tasa de comisión real del proveedor.

### ADR-2 — Ambigüedad real (≥2 combinaciones cuadran la misma suma): requiere decisión humana, nunca auto-resolución arbitraria

**Decisión:** cuando el subset-sum de conciliación N-a-1 encuentra 2 o más subconjuntos distintos cuya suma cae dentro de la banda de tolerancia para el mismo depósito bancario (REQ-CONC-007, REQ-CONC-008), ninguno se aplica automáticamente. El caso queda en `estado="sugerido"` listando todos los candidatos con su score, y solo un humano —vía un endpoint de confirmación explícita— decide cuál aplicar (o ninguno).

**Por qué:**
- Elegir arbitrariamente entre 2 combinaciones matemáticamente válidas (p. ej. por orden de inserción o por ser la primera encontrada) es una decisión de negocio disfrazada de decisión técnica: cuál factura quedó realmente pagada tiene efecto en AR/AP, en el estado de cobranza del cliente y potencialmente en el reconocimiento de ingreso.
- El costo de pedirle a un humano que desambigüe 1 caso en 100 es bajo comparado con el costo de conciliar mal una factura real contra el depósito equivocado.
- Aplica también, con la misma lógica, a la migración de catálogo de cuentas: un match `fuzzy` con 2 candidatos de score similar (REQ-MIG-005) nunca se autoaprueba solo por tener el score más alto de los dos.

**Alternativas descartadas:**
- Resolver por "primer candidato generado" (orden determinista del algoritmo): descartada porque el orden de generación del subset-sum es un detalle de implementación, no una señal de negocio — cambiar el algoritmo internamente cambiaría silenciosamente qué facturas se marcan pagadas.
- Resolver por candidato de mayor score de desambiguación (REQ-CONC-009) sin intervención humana, con umbral bajo: descartada porque el score de desambiguación es una heurística (comisión esperada, compacidad de fechas), no una prueba; usarlo para decidir automáticamente en un empate cercano reintroduce el mismo riesgo que el score fue diseñado para exponer, no para ocultar.

### ADR-3 — Nunca migrar/reclasificar una cuenta del catálogo sin aprobación humana explícita

**Decisión:** ningún `MapeoMigracionCuenta` con `tipo_match` distinto de `"exacto"` (es decir, `alerta_riesgo`, `fuzzy`, o cualquier caso de fusión N:1) puede usarse para migrar una sola póliza histórica sin haber pasado primero por `estado="aprobado"` vía el endpoint de revisión humana (REQ-MIG-007). El motor de migración de pólizas (REQ-MIG-009) valida esto en tiempo de ejecución, no solo en la UI.

**Por qué:**
- Una reclasificación de cuenta incorrecta puede alterar retroactivamente el ISR/IVA ya declarado de periodos cerrados, o invalidar contabilidad electrónica ya dictaminada ante el SAT — el propio caso de negocio lo señala como el riesgo central de esta feature.
- El match exacto (código+nombre normalizados idénticos) es la única categoría sin riesgo de ambigüedad real: dos cuentas con el mismo código y el mismo nombre en ambos sistemas son, para efectos prácticos, la misma cuenta. Todo lo demás implica una decisión humana sobre "esto representa lo mismo".
- Este principio es el mismo que ya aplica en este repo a la reclasificación de CFDI individuales (`b2b_ai/features/bookkeeping/human_override.py::OverrideAction.RECLASSIFY`, que exige acción explícita); aquí se extiende de "reclasificar un documento suelto" a "reclasificar una cuenta completa del catálogo con todo su histórico".

**Alternativas descartadas:**
- Auto-aprobar matches fuzzy por encima de un score muy alto (p. ej. `score >= 95`): descartada porque incluso un score alto de similitud de texto no verifica que las dos cuentas compartan naturaleza, nivel jerárquico y tratamiento fiscal — dos cuentas con nombres casi idénticos pueden ser fiscalmente distintas (p. ej. "Bancos MXN" vs. "Bancos USD").
- Requerir aprobación solo a nivel de "familia de cuentas" (aprobar en bloque todo un capítulo del catálogo): descartada para la primera versión porque diluye la responsabilidad de la aprobación — el contador podría aprobar 40 cuentas de golpe sin revisar el caso puntual de alerta de riesgo que sí necesitaba su atención.

### ADR-4 — El sistema nunca aplica la presunción del Art. 59 fracción III CFF por su cuenta

**Decisión:** el motor de clasificación de depósitos (financiamiento/aportación de socio/garantía) y el flujo de devolución de IVA nunca marcan automáticamente una solicitud como `"rechazada"` o reducen el monto solicitado solo por detectar depósitos sin documento de soporte suficiente. El máximo que el sistema hace por sí mismo es marcar `requiere_revision_humana=True` con la cita del criterio PRODECON 1/2026 (la autoridad no puede aplicar la presunción del Art. 59 fr. III CFF sin ejercer antes facultades de comprobación).

**Por qué:**
- Replicar dentro del software una negación que ni el propio SAT puede aplicar sin abrir una auditoría formal expondría al despacho a construir un flujo más agresivo que la ley, con el riesgo de recomendar al cliente retirar o reducir una solicitud de devolución legítima.
- El rol del software aquí es preparar evidencia (documento + registro contable + trazabilidad del pago) para sostener la clasificación ante una eventual revisión, no pre-juzgar al contribuyente.

**Alternativas descartadas:**
- Bloquear la solicitud de devolución completa si hay cualquier depósito sin clasificar: descartada por ser desproporcionada — un solo depósito ambiguo no debería impedir generar el papel de trabajo completo, solo debe señalarse como pendiente de evidencia dentro de la sección 7 del papel de trabajo.

### ADR-5 — Persistencia real desde el día uno para las tres features, no dicts en memoria

**Decisión:** las tres features de este blueprint (`devolucion_iva`, `reconciliacion_ingresos_egresos`, `migracion_catalogo`, y la extensión de `bank_reconciliation`) se diseñan con tablas en Postgres desde el primer requisito de persistencia (REQ-IVA-005/006, REQ-MIG-001/002, REQ-CONC-013), no como una fase posterior "cuando escale".

**Por qué:**
- El estado actual de `devolucion_iva` y `reconciliacion_ingresos_egresos` (dicts a nivel de módulo/instancia) ya demostró en la investigación un bug real de código muerto (`self._conciliaciones` nunca se asigna, REQ-IVA-012) y una fuga potencial multi-tenant (`listar_solicitudes` ignora el filtro, REQ-IVA-007) — ambos son síntomas típicos de almacenamiento en memoria sin disciplina de acceso por tenant, y volver a repetir el patrón en las 2 features nuevas garantizaría los mismos bugs.
- Una solicitud de devolución de IVA o una migración de catálogo son operaciones que un despacho necesita poder auditar semanas o meses después — no son estado efímero de una sesión.

**Alternativas descartadas:**
- Empezar en memoria "para MVP rápido" y migrar a BD después: descartada explícitamente porque el propio código existente muestra que esa migración nunca ocurrió para `devolucion_iva`/`reconciliacion_ingresos_egresos` a pesar de estar en producción conceptual — el riesgo de que "después" nunca llegue es alto y ya se materializó una vez en este mismo repo.
